# Changelog

## v0.0.8 (2026-09-03)

### 提示词体系重构（破坏性：旧存档的提示词部分不兼容）
- 删除提示词编辑界面（行内/悬浮两处 tab）与整套多配置体系（配置快照、聊天/角色绑定、导入导出、拖拽排序）——提示词正文不再存档、不再有 UI，由维护者直接改代码
- 提示词模块唯一来源改为根目录 `default-prompt-modules.json`（原 `choice-prompts-optimized.json` 更名）：core_rules 的三段拼接（option_rules / person_style / CORE_RULES_STATIC 常量 + generator 运行时组装）合并为 JSON 内完整正文
- 清除提示词正文中的"输入润色"模式残留（system_prompt 双模式介绍、assistant_ack 模式判断话术）——润色功能早已删除，但该文本此前一直随提示词真实发送
- 移除柏宝书桥接全链路（baibai-bridge.ts、baibai_enabled 开关、baibai_summary/baibai_state 模块）
- 上下文模式/轮数与预填充开关迁入「生成」tab（原提示词页顶栏），运行时语义不变；`prompt_rules` 存档收缩为纯运行时参数（上下文/预填充/人称/字数）
- 删除提示词 17 版迁移链与 v19 配置分流（约 620 行），旧存档相关字段由 Zod 直接剥离；连带清除 option_only 死字段、孤儿 ChatFilterGroup schema、零引用的 store 导出
- global-settings store 从 1224 行瘦身至约 470 行

### 类型检查
- 新增 `tsconfig.typecheck.json`：`@sillytavern/*` 类型映射指向 `../TauriTavern/src`（实际使用的酒馆分支源码），开发克隆内可跑 vue-tsc
- 修复 `default-pool.ts` 失效的类型导入（`'./settings'`→`'@/type/settings'`，此前被 import type 编译期擦除掩盖）

### 文档
- AGENTS.md 同步新架构（提示词单源约束、类型检查说明、目录清单），清理已删功能的过时文案（输入润色、目录中不存在的文件）

## v0.0.7 (2026-08-29)

### 提示词导入
- 导入改为合并/替换双模式：默认合并（同 ID 覆盖内容并保留本地顺序，新 ID 追加末尾），可选整体替换；导入前弹预览确认框，显示覆盖/新增/保留计数，替代原生 confirm
- 导入落盘修复：导入内容写入正在编辑的配置快照——此前只写工作副本，下次切换配置会被同步回"生效配置"（可能是另一个配置）导致内容静默写错位置或丢失
- 新增工作副本归属跟踪（promptEditConfigId）：切换配置时回写命中"最近编辑的配置"而非"生效配置"（聊天/角色绑定决定）；下拉选中 ≠ 生效配置时，编辑内容不再静默串写

### 配置语义
- 全新安装/恢复出厂仅创建一个「简洁」默认配置；「经典」只作为老存档迁移时用户已有提示词的存档存在，不再把内置默认伪装成经典预设
- 出厂默认提示词内容统一为简洁基准（core_rules/thinking_prompt/enrich_core_rules/enrich_thinking 四模块），"恢复默认"单模块/全部内容/完全恢复基准随之统一
- 清理 4 个零引用的旧默认文本死常量（DEFAULT_AI_PERSONA/DEFAULT_PERSON/DEFAULT_PROMPT_OUTPUT_FORMAT/DEFAULT_PROMPT_EXTRA）

### 移动端
- 修复横滑 tab 栏带动内容区/页面滚动：滚动收容 + 激活 tab 自动居中

### 修复
- 提示词导入弹窗补 footer（取消/确认按钮），此前弹窗无确认入口

## v0.0.6 (2026-08-29)

### 条目池生成
- AI 生成输出契约改为 JSON 结构化数据（type/content/rule/replace），类型标签正确解析进类型框，不再整行塞进「AI 生成指令」
- AI 可为每条生成条目补充使用规则（rule），结果行可直接修改，注入所见即所得
- 新增可选「目标类型」输入：填写后双路（system+user）强制所有生成条目使用该类型，防止「选项指导」跑偏成具体选项；留空由 AI 按生成要求判断
- 条目种类跟随生成要求：要求「选项指导」即产出指导/约束文本，不再被硬编码成行动方向；留空默认保持简洁行动方向
- 替换建议携带新的 type/content/rule；AI 未给规则时结果行预填原条目规则，避免注入静默清空
- 回退解析（模型无视 JSON 时）支持 `【类型】`/`[类型]`/`短标签：` 三种类型前缀，与喂给 AI 的已有条目格式 `[type] content` 往返一致
- 「生成要求」输入框改为引导式 placeholder

### 修复
- 尾随逗号清理改为字符串感知扫描：条目正文字面量中的 `,]`/`,}` 不再被误删（仅条目池生成路径；行动选项的 parseOptions 同类正则未动）
- vue-tsc 类型基线修复：`settings.ts`/`util/zod.ts` 显式导入 zod——auto-import 生成的全局 `z` 在类型位置不可用作命名空间（TS 6.0 nightly 实测 TS2503），曾造成全仓 170+ 行级联类型错误

## v0.0.5 (2026-08-29)

### 正则库
- 从酒馆导入：正则库一键读取酒馆三区正则脚本（全局 / 对话补全预设 / 角色卡局部），无需手动复制
- 「仅显示美化」（markdownOnly）脚本禁止导入——其只改聊天显示，导入为过滤规则会改废 AI 看到的正文
- 导入目标为正则库内部分组：可选现有分组或新建分组（带重名检测，按来源预填分组名）
- 重复条目自动去重跳过，导入结果按已导入 / 跳过重复 / 无效项分别提示

### 过滤
- 正则规则新增「替换为」字段：语义与酒馆 `replaceString` 一致，支持 `$1` 捕获组引用（如去标签壳保留内容）；留空 = 整段删除（原行为）
- 从酒馆导入的正则自动带入其 `replaceString`；旧存档规则自动补空值，行为不变

### 修复
- 角色卡绑定分组失配：酒馆 1.18 实测 `this_chid` 为字符串而 schema 声明为 number，运行时写入字符串 ID 后重载会 Zod 抛错导致插件启动失败；现统一归一化为字符串，新旧存档兼容
- 旧版平铺过滤列表迁移出的分组缺省绑定字段，曾被分区判定误当成预设/角色卡绑定；现显式置空

### UI
- 拖拽排序统一收敛到共享配置：正则库分组/条目、过滤编辑器、条目池等弹窗的拖拽行为（悬停自动展开折叠分组等）保持一致
- 补齐 87 条英文翻译（含从酒馆导入弹窗全部字符串）

## v0.0.4 (2026-08-28)

### 提示词
- 提示词多配置系统：自动迁移创建「经典/简洁」双配置，支持新建/重命名/删除/设为默认，聊天 > 角色 > 默认三层绑定
- 移除 `baibai_state` 模块（旧配置残留自动隐藏，不再注入）

### 过滤
- 聊天记录过滤独立成「过滤」tab：全局/预设/角色卡三区域分组，分组可绑定 ST 对话补全预设与角色卡
- 正则库：标签/正则两类可复用条目，分组管理（新建/重命名/删除/排序），分组条目支持引用库条目或内联规则
- 正则库条目与分组拖拽排序、跨组移动、拖拽悬停自动展开折叠分组
- 启用分组置顶显示、「绑定到当前预设」快捷操作、删除库条目后自动清理分组内孤儿引用
- 旧版过滤分组数据自动迁移到新存储，升级不丢数据

### UI
- 确认弹窗支持隐藏取消按钮
- 过滤编辑器锁定按钮着色提醒

### 修复
- `manifest.json` 版本号修复（0.0.2 → 0.0.4），此前两次发布漏 bump 导致 `auto_update` 无法推送更新
- 「恢复默认」单模块/全部模块/整体恢复三处因缺失常量导入在点击时崩溃（v0.0.2 起存在）
- 提示词配置切换与删除因缺失 store 导入崩溃
- 旧过滤分组未搬运到新存储，导致升级后分组从 UI 消失
- 恢复默认后新建正则库分组写入静默丢失
- 窄屏（<420px，手机 WebView）下悬浮设置面板横向出界
- 补齐 V4 新增界面共 57 条英文翻译

## v0.0.3 (2026-08-28)

### UI
- 提示词页面工具栏重构：拆分为页面设置栏（轮数模式/预填充/柏宝书/预览）和模块操作栏（模式切换/新增/导出/导入/恢复默认），恢复默认按钮紧贴模块列表，避免误认为全页重置
- 新增模块合并为下拉按钮：通用模块、选项模块、润色模块三种类型统一入口，补齐缺失的选项模块创建能力

### 提示词
- Persona Description 模块移至"参考开始"之后，输出包裹 `<user_persona>` XML 标签，明确标注为"用户本人（用户=主角=user）的人物设定"，与角色卡设定区分

## v0.0.2 (2026-08-27)

### 核心功能
- 主题系统重构：实色三层配色，ST 跟随模式（`auto`/`st`/`dark`/`light` 四模），对比度守卫
- 条目池数据模型重构：PoolEntry `text` → `type` + `content` + `rule` 三字段
- 条目池导入导出：合并/替换双模式，单键导出 JSON
- 润色/选项生成 prompt 灵活化：去掉死板模板，精简规则
- 柏宝书提示词标签化：`baibai_summary` / `baibai_state` 模块，开关控制
- 润色计数：`enrich_count` 改为 string 类型，支持 `"4"` 或 `"3-6"` 区间，空值返回 0

### UI
- 原生控件样式接管（`global.css`），保持与 ST 主题一致
- API 编辑器交互优化
- 模块计数修复（柏宝书过滤后计数准确）
- 字体一致性：硬编码字号替换为 `--choice-text-*` 设计令牌
- 聊天记录过滤默认折叠

### 修复
- 贴边气泡弹跳修复 + 面板出界修复
- 固定条目溢出策略从配置中移除 `pinned_follows_condition`
- 旧数据迁移：`text`→`type`、`enrich_count` number→string、`theme`→`theme_mode`、`entry_ids`→`entries`

## v0.0.1 (2026-08-26)

### 核心功能
- 行动选项异步生成：独立 API 调用，不影响主回复
- 条目池总条目库 + 配置架构，支持聊天/角色/默认三层绑定
- 提示词模块化编辑器：拖拽排序、复制、删除、恢复
- 悬浮球状态指示器：Idle/Generating/Disabled 状态，长按菜单，边缘吸附
- 输入润色：AI 改写用户输入为多个版本
- 世界书注入：复用 ST 原生 `getWorldInfoPrompt`
- AI 生成条目池：批量生成候选条目
- 聊天记录过滤：标签/正则规则分组

### UI
- 设计令牌体系：统一间距/字号/z-index 变量
- 悬浮球状态机改造、FloatingContextMenu
- 选项面板支持 compact 模式
- 新手指南页面
- 响应式布局（ResizeObserver，~380px 兼容）

### 修复
- 世界书注入预算问题（maxCtx 从 8192 放宽到 128000）
- 选项解析改为 JSON 格式，消除正则解析不稳定
- 思维链标签剥离正则修复
- 标题正则空格匹配（兼容全角/半角）
- 按钮竖向排列修复
- 提示词模块迁移（schema_version 0→16）